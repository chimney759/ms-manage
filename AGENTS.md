# 项目全局约束

本文件适用于本项目的所有页面、样式和交互改动。开始实现前与完成实现后都必须逐项校对。

## 模块边界

- 每个后台业务模块必须位于 `scripts/pages/` 中独立的工程文件；不得在已有业务模块文件中混入另一模块的页面实现。
- 通用导航、顶栏、提示和演示数据只放在 `scripts/components/layout.js`；页面路由只在 `scripts/app.js` 注册。
- 新增模块时：创建独立页面文件、在 `index.html` 按依赖顺序加载、在 `scripts/app.js` 注册路由，并保留既有导航与交互。
- 可复用的通用能力可以被业务模块调用，但不能以复制页面逻辑的方式造成多处维护。

## 样式与交互

- 样式必须使用所属模块的明确选择器前缀或组件类名，禁止使用会影响无关页面的宽泛标签选择器、无上下文的通用类覆盖或 `!important` 兜底。
- 修改样式前先搜索现有选择器，优先扩展当前模块规则；不得为解决局部问题覆盖其他模块的样式。
- 所有后台表单字段必须采用明确的“字段标签容器 + 控件容器”结构；必填标识、字段名和控件不得作为同一栅格容器的并列子项。复杂字段（版本范围、日期范围、人群分组）必须有独立的内部布局容器，禁止依赖 CSS Grid 自动放置。
- 完成涉及表单布局的改动前，必须逐项检查：标签与控件没有重叠、必填标识不脱离字段名、同组控件不遮挡、长占位文案可完整显示或合理截断，以及窄屏下布局可换行。
- 改动页面结构、表单或按钮时，必须检查默认态、编辑态、禁用态、hover/tooltip、保存、撤销和校验状态是否仍正常。
- 全局 tooltip 必须使用现有 `BackofficeLayout.bindGlobalTooltips()` 机制，确保不会被容器裁切。
- 保持后台界面紧凑、可扫描，沿用现有间距、边框和按钮规范；不引入与当前页面不一致的装饰性卡片或布局。

## 演示数据与保存

- 用于演示的业务操作数据必须具备持久化能力：用户完成页面既有保存动作后，数据需写入浏览器本地存储，并在刷新或切换页面后恢复。
- 草稿、组件保存和页面保存必须遵循页面既有的保存层级；不得以自动持久化绕过最终页面保存，也不得让撤销动作恢复未提交的草稿。
- 涉及用户上传的演示素材时，持久化数据不得依赖刷新后会失效的 `blob:` URL；应使用可恢复的数据 URL 或该模块既有的持久化方案。

## 变更校验

- 每次改动后必须执行受影响脚本的 `node --check`；涉及多模块或路由时，对全部 `scripts/**/*.js` 执行语法检查。
- 每次改动后必须执行 `git diff --check`。
- 变更前后均检查 `git status --short`，保留用户已有的未提交改动，不得擅自回退或覆盖。
- 有可用浏览器验证条件时，必须检查对应页面的桌面与窄屏布局、导航入口、关键操作路径与文本溢出；未执行时需明确说明。
- 未经用户明确要求，不提交、不推送、不发布。
