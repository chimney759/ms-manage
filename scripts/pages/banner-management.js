window.BannerManagementPage = {
  storageKey: 'meiyou-cashback-banner-management',
  audienceOptions: ['返现新用户（实时）', '首次访问用户（实时）', '0单用户群（实时）', '≤1单的用户群（实时）', '淘宝0单用户群（实时）', '抖音0单用户群（实时）', '返现uid尾号为双号人群（实时）', '返现uid尾号为单号人群（实时）', '临时人群 - ≤1单的用户群且有待使用返现红包的用户群（实时）', '活动人群-可领取现金红包的用户群', '活动人群-可使用现金红包的用户群', '活动人群-现金红包待到账的用户群', '活动人群-现金红包已到账的用户群', '活动人群-当日已访问过大促活动页面用户群'],
  seedRows: [{ id: '24', name: '【首页实验】新手教程_0709', position: '首页', scenes: ['首页'], sortValue: 200, image: 'https://image.fanhuan.com/cp/img/2d8ed65671f0dafb73b6fea1de920662_1125_156.png', targeting: { audiences: ['≤1单的用户群（实时）'], experimentId: '2322-13883,2322-13884,2322-13891' }, onlineAt: '2026-07-09T12:00', offlineAt: '2026-12-31T23:59', status: '已下线', creator: '郑敏妃', editor: '郑敏妃', updatedAt: '2026-08-05T15:01:09' }, { id: '23', name: '首页返现新人横幅', position: '首页', scenes: ['首页', '搜索中间页'], sortValue: 160, image: 'https://image.fanhuan.com/cp/img/dd840f593f62c5c811684cde7dce20f6_270_270.gif', targeting: { targetGroup: '新客白名单' }, onlineAt: '2026-08-15T10:00', offlineAt: '2026-09-30T23:59', status: '上线中', creator: '张文贞', editor: '张文贞', updatedAt: '2026-08-18T10:21:09' }, { id: '22', name: '订单详情页大促横幅', position: '首页', scenes: ['订单详情页'], sortValue: 120, image: 'https://image.fanhuan.com/cp/img/2a109c6824a9995f623cf398963b5c71_270_270.gif', targeting: {}, onlineAt: '2026-09-10T00:00', offlineAt: '2026-09-20T23:59', status: '待上线', creator: '罗至玲', editor: '王敏', updatedAt: '2026-08-27T17:43:12' }],
  clone(value) { return JSON.parse(JSON.stringify(value)); },
  escape(value) { return String(value ?? '').replace(/[&<>'"]/g, (item) => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;' })[item]); },
  read() { try { const saved = JSON.parse(localStorage.getItem(this.storageKey)); if (Array.isArray(saved)) return saved; } catch (error) { /* use demonstrative data */ } return this.clone(this.seedRows); },
  write(rows) { localStorage.setItem(this.storageKey, JSON.stringify(rows)); },
  formatDate(value) { return value ? `${value.replace('T', ' ')}${value.length === 16 ? ':00' : ''}` : '-'; },
  statusClass(value) { return value === '上线中' ? 'online' : value === '待上线' ? 'pending' : 'offline'; },
  sortIcon(direction) { const path = direction === 'asc' ? 'm4.5 9.5 3.5-3.5 3.5 3.5' : direction === 'desc' ? 'm4.5 6.5 3.5 3.5 3.5-3.5' : 'm4.75 6.25 3.25-3.25 3.25 3.25M4.75 9.75 8 13l3.25-3.25'; return `<svg viewBox="0 0 16 16" aria-hidden="true" focusable="false"><path d="${path}" /></svg>`; },
  targetingSummary(targeting = {}) { const values = []; if (targeting.targetGroup) values.push(`指定人群包：${targeting.targetGroup}`); if (targeting.audiences?.length) values.push(`定制人群：${targeting.audiences.join('、')}`); if (targeting.experimentId) values.push(`指定实验可见：${targeting.experimentId}`); return values.join('；') || '全部用户'; },
  render({ mode = 'list', recordId = '', copy = false } = {}) {
    const title = mode === 'editor' ? `${copy ? '复制' : recordId ? '编辑' : '新增'}横幅` : '';
    return `<section class="content banner-management-page${mode === 'editor' ? ' is-banner-editor' : ''}">${mode === 'editor' ? `<header class="banner-editor-heading"><button type="button" class="banner-back" data-banner-back aria-label="返回横幅列表" title="返回横幅列表"><svg viewBox="0 0 16 16" aria-hidden="true"><path d="M9.75 3.5 5.25 8l4.5 4.5" /></svg></button><h1>${title}</h1></header>` : ''}<section class="banner-management-workspace panel"><div id="banner-management-body" class="banner-management-body"></div></section></section>`;
  },
  renderList(rows, filters, sort, page) {
    const root = document.getElementById('banner-management-body'); const direction = sort.key === 'sortValue' ? (sort.direction === 1 ? 'asc' : 'desc') : 'none';
    root.innerHTML = `<header class="marketing-workspace-heading banner-list-heading"><div><h1>横幅列表</h1><span class="heading-note">运营横幅的投放配置与展示规则</span></div><button class="button primary" type="button" data-banner-add>新增横幅</button></header><div class="banner-list-filters"><label><span>业务</span><input class="control" value="美柚省钱App" disabled /></label><label><span>位置</span><input class="control" value="首页" disabled /></label><label><span>记录名称</span><input class="control" data-banner-filter="name" value="${this.escape(filters.name)}" placeholder="请输入名称进行搜索" /></label><label><span>状态</span><select class="control" data-banner-filter="status"><option value="">请筛选状态</option>${['上线中', '待上线', '已下线'].map((item) => `<option value="${item}"${filters.status === item ? ' selected' : ''}>${item}</option>`).join('')}</select></label><label class="banner-filter-date"><span>生效时间</span><div><input class="control" type="datetime-local" data-banner-filter="start" value="${this.escape(filters.start)}" /><i>至</i><input class="control" type="datetime-local" data-banner-filter="end" value="${this.escape(filters.end)}" /></div></label><div class="banner-filter-actions"><button class="button secondary" type="button" data-banner-search>查询</button></div></div><div class="banner-table-wrap"><table class="banner-table"><thead><tr><th>ID</th><th>名称</th><th>场景</th><th><button class="banner-sort" type="button" data-banner-sort aria-sort="${direction}"><span>排序值</span>${this.sortIcon(direction)}</button></th><th>横幅图片</th><th>定向信息</th><th>上线时间</th><th>下线时间</th><th>状态</th><th>创建人</th><th>最后编辑</th><th>最后更新时间</th><th>操作</th></tr></thead><tbody data-banner-table-body></tbody></table></div><footer class="banner-list-footer"><span data-banner-count></span><div class="banner-pagination"><span>20条/页</span><button type="button" disabled aria-label="上一页">‹</button><button type="button" class="is-active">${page}</button><button type="button" disabled aria-label="下一页">›</button><label>前往 <input class="control" value="${page}" aria-label="前往页码" /> 页</label></div></footer>`;
    const visible = rows.filter((row) => (!filters.name || row.name.toLowerCase().includes(filters.name.trim().toLowerCase())) && (!filters.status || row.status === filters.status) && (!filters.start || row.offlineAt >= filters.start) && (!filters.end || row.onlineAt <= filters.end));
    if (sort.key === 'sortValue') visible.sort((a, b) => (Number(a.sortValue) - Number(b.sortValue)) * sort.direction);
    root.querySelector('[data-banner-table-body]').innerHTML = visible.length ? visible.map((row) => `<tr data-banner-id="${this.escape(row.id)}"><td>${this.escape(row.id)}</td><td class="banner-name-cell">${this.escape(row.name)}</td><td>${this.escape((row.scenes || []).join('、'))}</td><td>${this.escape(row.sortValue)}</td><td>${row.image ? `<span class="banner-image-trigger" data-banner-image="${this.escape(row.image)}"><img src="${this.escape(row.image)}" alt="${this.escape(row.name)}横幅图片" /></span>` : '<span class="banner-image-empty">暂无图片</span>'}</td><td><span class="banner-targeting-summary" title="${this.escape(this.targetingSummary(row.targeting))}">${this.escape(this.targetingSummary(row.targeting))}</span></td><td>${this.formatDate(row.onlineAt)}</td><td>${this.formatDate(row.offlineAt)}</td><td>${window.BackofficeLayout.statusTag(row.status)}</td><td>${this.escape(row.creator)}</td><td>${this.escape(row.editor)}</td><td>${this.escape(row.updatedAt || '-')}</td><td><span class="banner-actions"><button class="text-button" type="button" data-banner-edit>编辑</button><button class="text-button" type="button" data-banner-copy>复制</button></span></td></tr>`).join('') : '<tr><td class="banner-empty" colspan="13">暂无符合条件的横幅配置</td></tr>';
    root.querySelector('[data-banner-count]').textContent = `共 ${visible.length} 条`;
  },
  emptyRecord() { return { id: '', name: '', position: '首页', scenes: [], sortValue: '', image: '', darkImage: '', route: { type: 'protocol', protocol: '', pid: '', selectedPid: '', skipType: '', mallId: '', outboundLogo: '', outboundCopy: '' }, targeting: { identities: [], targetGroup: '', excludeGroup: '', audiences: [], audienceInversion: '否', experimentId: '', excludeExperiment: '', platformVersions: { ios: { enabled: true, start: '8.96.0.0', end: '' }, android: { enabled: true, start: '8.96.0.0', end: '' }, harmony: { enabled: true, start: '8.96.0.0', end: '' } } }, onlineAt: '', offlineAt: '', status: '待上线', testPlan: { uids: '', start: '', end: '', enabled: false }, creator: '管理员', editor: '管理员', updatedAt: '' }; },
  renderEditor(record, { copy = false } = {}) {
    const root = document.getElementById('banner-management-body'); const field = (label, control, className = '') => `<div class="config-field ${className}"><span class="config-field-label">${label}</span><div class="config-field-control">${control}</div></div>`; const required = '<b class="field-required">*</b>';
    const route = { ...this.emptyRecord().route, ...(record.route || {}) }; const targeting = { ...this.emptyRecord().targeting, ...(record.targeting || {}), platformVersions: { ...this.emptyRecord().targeting.platformVersions, ...(record.targeting?.platformVersions || {}) } }; const test = { ...this.emptyRecord().testPlan, ...(record.testPlan || {}) };
    const asset = (key, label, value, requiredAsset = false) => field(`${requiredAsset ? required : ''}${label}`, `<div class="banner-asset"><span class="banner-asset-preview">${value ? `<img src="${this.escape(value)}" alt="${label}" />` : '<b>图片</b>'}</span><span class="banner-asset-actions"><label class="button secondary banner-upload">上传图片<input type="file" accept="image/*" data-banner-upload="${key}" /></label><button type="button" class="banner-delete-image" data-banner-delete="${key}"${value ? '' : ' disabled'}>删除图片</button></span><input type="hidden" name="${key}" value="${this.escape(value)}" /></div>`, 'banner-asset-field');
    const platform = (key, label) => { const item = { enabled: true, start: '', end: '', ...(targeting.platformVersions[key] || {}) }; return `<div class="banner-platform-row"><label><input type="checkbox" data-banner-platform="${key}"${item.enabled ? ' checked' : ''} />${label}</label><input class="control" data-banner-version="${key}:start" value="${this.escape(item.start)}" placeholder="最低版本" /><span>至</span><input class="control" data-banner-version="${key}:end" value="${this.escape(item.end)}" placeholder="最高版本（选填）" /></div>`; };
    const bannerScenes = [
      { label: '首页', note: '慎用，可能影响其他资源位曝光', tooltip: '首页横幅场景慎用，可能影响其他资源位曝光。' },
      { label: '搜索中间页', tooltip: '横幅将展示在搜索中间页。' },
      { label: '搜索结果页', tooltip: '横幅将展示在搜索结果页。' },
      { label: '我的返现页', tooltip: '横幅将展示在我的返现页。' },
      { label: '订单详情页', tooltip: '横幅将展示在订单详情页。' }
    ];
    const bannerSceneControl = `<span class="banner-scene-options">${bannerScenes.map((item) => `<label><input type="checkbox" data-banner-scene value="${item.label}"${record.scenes?.includes(item.label) ? ' checked' : ''} /><span>${item.label}${item.note ? `（${item.note}）` : ''}</span><button class="help-tooltip" type="button" aria-label="${item.label}说明" data-tooltip="${item.tooltip}">?</button></label>`).join('')}</span>`;
    root.innerHTML = `<header class="banner-form-title"><div><h2>${copy ? '复制横幅' : record.id ? '编辑横幅' : '新增横幅'}</h2><span>配置横幅的素材、跳转方式和投放规则</span></div></header><form class="banner-form" data-banner-form novalidate><section class="shared-config-section"><h3>基础信息</h3>${field('业务', '<input class="control" value="美柚-返现" disabled />')}${field('位置', '<input class="control" value="首页" disabled />')}${field(`${required}记录名称`, `<input class="control" name="name" value="${this.escape(record.name)}" maxlength="60" placeholder="仅用于后台记录，前台不可见" />`)}${field(`${required}排序 <button class="help-tooltip" type="button" aria-label="排序说明" data-tooltip="排序越大展示越靠前。">?</button>`, `<input class="control" name="sortValue" type="number" min="0" step="1" value="${this.escape(record.sortValue)}" placeholder="越大展示越靠前" />`)}${field(`${required}横幅场景`, `<span class="banner-scene-options">${['首页', '搜索中间页', '搜索结果页', '我的返现页', '订单详情页'].map((item) => `<label><input type="checkbox" data-banner-scene value="${item}"${record.scenes?.includes(item) ? ' checked' : ''} /><span>${item}</span>${item === '首页' ? '<small>慎用，可能影响其他资源位曝光</small>' : ''}</label>`).join('')}</span>`)}</section><section class="shared-config-section"><h3>素材</h3>${asset('image', '横幅素材', record.image, true)}${asset('darkImage', '暗黑模式', record.darkImage)}</section><section class="shared-config-section"><h3>跳转配置</h3><p class="banner-section-note">路由协议填写示例：请填写可被客户端识别的页面路由或自定义协议。</p>${field('跳转类型', `<select class="control" name="routeType" data-banner-route-type><option value="">请选择跳转类型</option><option value="page"${route.type === 'page' ? ' selected' : ''}>页面跳转</option><option value="protocol"${route.type === 'protocol' ? ' selected' : ''}>自定义地址/协议</option></select>`)}<div class="banner-route-config" data-banner-route${route.type ? '' : ' hidden'}>${field('路由协议', `<input class="control" name="routeProtocol" value="${this.escape(route.protocol)}" placeholder="请输入路由协议" />`)}${field('PID', `<input class="control" name="routePid" value="${this.escape(route.pid)}" placeholder="pid(除京东&拼多多&抖音&1688，其余商城用于埋点上报)" />`)}${field('PID选择', `<select class="control" name="routeSelectedPid"><option value="">请选择pid</option><option value="default"${route.selectedPid === 'default' ? ' selected' : ''}>默认PID</option><option value="custom"${route.selectedPid === 'custom' ? ' selected' : ''}>自定义PID</option></select>`)}${field('skip_type', `<input class="control" name="routeSkipType" value="${this.escape(route.skipType)}" placeholder="skip_type(用于埋点上报)" />`)}${field('商城id', `<input class="control" name="routeMallId" value="${this.escape(route.mallId)}" placeholder="请输入商城id" />`)}${asset('outboundLogo', '出站弹窗logo', route.outboundLogo)}${field('出站弹窗文案', `<input class="control" name="routeOutboundCopy" value="${this.escape(route.outboundCopy)}" maxlength="80" placeholder="请输入出站弹窗文案" />`)}</div></section><section class="shared-config-section"><h3>定向信息</h3>${field('指定人群身份', `<span class="banner-identity-options">${['经期', '怀孕', '备孕', '辣妈', '亲友'].map((item) => `<label><input type="checkbox" data-banner-identity value="${item}"${targeting.identities?.includes(item) ? ' checked' : ''} />${item}</label>`).join('')}</span>`)}${field('指定人群包', `<input class="control" data-banner-targeting="targetGroup" value="${this.escape(targeting.targetGroup)}" placeholder="填入表名，不填默认全部用户" />`)}${field('排除人群包', `<input class="control" data-banner-targeting="excludeGroup" value="${this.escape(targeting.excludeGroup)}" placeholder="填入表名，不填默认为空" />`)}${field('定制人群', `<div class="banner-audience-options">${this.audienceOptions.map((item) => `<label><input type="checkbox" data-banner-audience value="${item}"${targeting.audiences?.includes(item) ? ' checked' : ''} />${item}</label>`).join('')}</div>`, 'banner-audience-field')}${field('是否定制人群取反', `<span class="home-entry-status-control"><label><input type="radio" name="audienceInversion" value="否"${targeting.audienceInversion === '否' ? ' checked' : ''} />否</label><label><input type="radio" name="audienceInversion" value="是"${targeting.audienceInversion === '是' ? ' checked' : ''} />是</label><small class="banner-inline-note">选择定制人群后，取反表示圈定人群以外的用户。</small></span>`)}${field('指定实验可见', `<input class="control" data-banner-targeting="experimentId" value="${this.escape(targeting.experimentId)}" placeholder="配置格式A-B，英文逗号隔开，如：1338-3550,1339-3510" />`)}${field('排除实验', `<input class="control" data-banner-targeting="excludeExperiment" value="${this.escape(targeting.excludeExperiment)}" placeholder="配置格式A-B，英文逗号隔开，如：1338-3550,1339-3510" />`)}${field(`${required}平台和版本`, `<div class="banner-platform-list">${platform('ios', 'iOS')}${platform('android', 'Android')}${platform('harmony', 'Harmony')}<p>仅适用于 8.96.0.0 及以上版本</p></div>`, 'banner-platform-field')}</section><section class="shared-config-section"><h3>投放设置</h3>${field(`${required}上线时间`, `<div class="config-date-range"><label><span>开始</span><input class="control" name="onlineAt" type="datetime-local" value="${this.escape(record.onlineAt)}" /></label><label><span>结束</span><input class="control" name="offlineAt" type="datetime-local" value="${this.escape(record.offlineAt)}" /></label></div>`)}${field(`${required}状态`, `<span class="home-entry-status-control"><label><input type="radio" name="status" value="上线中"${record.status === '上线中' ? ' checked' : ''} />上线</label><label><input type="radio" name="status" value="待上线"${record.status === '待上线' ? ' checked' : ''} />待上线</label><label><input type="radio" name="status" value="已下线"${record.status === '已下线' ? ' checked' : ''} />下线</label></span>`)}</section><section class="shared-config-section banner-test-plan"><h3>测试计划</h3><p>测试 UID 内的用户将在测试有效时间内看到此配置，到期自动终止，不影响正式配置。</p>${field('测试 UID', `<input class="control" data-banner-test="uids" value="${this.escape(test.uids)}" placeholder="请输入测试uid" />`)}${field('测试有效时间', `<div class="config-date-range"><label><span>开始</span><input class="control" data-banner-test="start" type="datetime-local" value="${this.escape(test.start)}" /></label><label><span>结束</span><input class="control" data-banner-test="end" type="datetime-local" value="${this.escape(test.end)}" /></label></div>`)}${field('测试状态', `<label class="banner-test-switch"><input type="checkbox" data-banner-test="enabled"${test.enabled ? ' checked' : ''} /><span class="switch-track"></span><b data-banner-test-status>${test.enabled ? '生效' : '未启用'}</b></label>`)}</section><footer class="banner-form-actions"><button class="button secondary" type="button" data-banner-cancel>取消</button><button class="button primary" type="submit">保存</button></footer></form>`;
  },
  bind({ mode = 'list', recordId = '', copy = false } = {}) {
    const rows = this.read(); const open = (nextMode, nextId = '', nextCopy = false) => { document.getElementById('page-root').innerHTML = this.render({ mode: nextMode, recordId: nextId, copy: nextCopy }); this.bind({ mode: nextMode, recordId: nextId, copy: nextCopy }); };
    if (mode === 'list') {
      const filters = { name: '', status: '', start: '', end: '' }; const sort = { key: '', direction: 1 }; let imagePreview; const hideImage = () => imagePreview?.remove(); const showImage = (trigger, event) => { hideImage(); const src = trigger.dataset.bannerImage; if (!src) return; imagePreview = document.createElement('div'); imagePreview.className = 'banner-image-popover'; imagePreview.innerHTML = `<img src="${this.escape(src)}" alt="横幅图片放大预览" />`; document.body.append(imagePreview); const rect = imagePreview.getBoundingClientRect(); imagePreview.style.left = `${Math.max(12, Math.min(event.clientX + 16, innerWidth - rect.width - 12))}px`; imagePreview.style.top = `${Math.max(12, Math.min(event.clientY + 16, innerHeight - rect.height - 12))}px`; };
      const refresh = () => this.renderList(rows, filters, sort, 1); refresh(); const root = document.getElementById('banner-management-body');
      root.addEventListener('input', (event) => { if (event.target.matches('[data-banner-filter]')) filters[event.target.dataset.bannerFilter] = event.target.value; }); root.addEventListener('change', (event) => { if (event.target.matches('[data-banner-filter]')) filters[event.target.dataset.bannerFilter] = event.target.value; });
      root.addEventListener('pointerover', (event) => { const item = event.target.closest('[data-banner-image]'); if (item && !item.contains(event.relatedTarget)) showImage(item, event); }); root.addEventListener('pointermove', (event) => { const item = event.target.closest('[data-banner-image]'); if (item) showImage(item, event); }); root.addEventListener('pointerout', (event) => { const item = event.target.closest('[data-banner-image]'); if (item && !item.contains(event.relatedTarget)) hideImage(); });
      root.addEventListener('click', (event) => { if (event.target.closest('[data-banner-add]')) return open('editor'); if (event.target.closest('[data-banner-search]')) return refresh(); if (event.target.closest('[data-banner-sort]')) { const current = sort.key === 'sortValue'; sort.key = 'sortValue'; sort.direction = current ? -sort.direction : 1; return refresh(); } const row = event.target.closest('[data-banner-id]'); if (row && event.target.closest('[data-banner-edit]')) open('editor', row.dataset.bannerId); if (row && event.target.closest('[data-banner-copy]')) open('editor', row.dataset.bannerId, true); });
      return;
    }
    const source = rows.find((row) => row.id === recordId); const record = source ? { ...this.emptyRecord(), ...this.clone(source) } : this.emptyRecord(); if (copy && source) { record.id = ''; record.name = `copy${source.name}`; record.creator = '管理员'; record.editor = '管理员'; } this.renderEditor(record, { copy }); const root = document.getElementById('banner-management-body'); const form = root.querySelector('[data-banner-form]'); const deliverySection = [...form.querySelectorAll('.shared-config-section')].find((section) => section.querySelector('h3')?.textContent === '投放设置'); deliverySection?.remove(); const targetingSection = [...form.querySelectorAll('.shared-config-section')].find((section) => section.querySelector('h3')?.textContent === '定向信息'); if (targetingSection) { const targeting = window.ConfigurationSections.normalizeTargeting({ ...this.emptyRecord().targeting, ...(record.targeting || {}), onlineStart: record.targeting?.onlineStart || record.onlineAt || '', onlineEnd: record.targeting?.onlineEnd || record.offlineAt || '', status: record.targeting?.status || record.status || '待上线', platformVersions: { ...this.emptyRecord().targeting.platformVersions, ...(record.targeting?.platformVersions || {}) } }); const audienceGroups = [{ title: '实时人群', items: this.audienceOptions.slice(0, 8) }, { title: '活动人群', items: this.audienceOptions.slice(8) }]; targetingSection.outerHTML = window.ConfigurationSections.renderTargeting({ prefix: 'banner', value: targeting, includePlatform: true, includeSchedule: true, required: true, identityLabel: '指定人群身份', identityOptions: ['经期', '怀孕', '备孕', '辣妈', '亲友'], audienceGroups, audienceInversionHint: '选择定制人群后，取反表示圈定人群以外的用户。', statusOptions: ['上线中', '待上线', '已下线'] }); } const sceneField = root.querySelector('.banner-scene-options'); if (sceneField) { const sceneDefinitions = [{ label: '首页', note: '慎用，可能影响其他资源位曝光', tooltip: '首页横幅场景慎用，可能影响其他资源位曝光。' }, { label: '搜索中间页', tooltip: '横幅将展示在搜索中间页。' }, { label: '搜索结果页', tooltip: '横幅将展示在搜索结果页。' }, { label: '我的返现页', tooltip: '我的返现页横幅将展示在我的返现页。' }, { label: '订单详情页', tooltip: '订单详情页横幅将展示在订单详情页。' }]; sceneField.innerHTML = sceneDefinitions.map((item) => `<label><input type="checkbox" data-banner-scene value="${item.label}"${record.scenes?.includes(item.label) ? ' checked' : ''} /><span>${item.label}${item.note ? `（${item.note}）` : ''}</span><button class="help-tooltip" type="button" aria-label="${item.label}说明" data-tooltip="${item.tooltip}">?</button></label>`).join(''); } const returnToList = () => open('list');
    document.querySelector('[data-banner-back]')?.addEventListener('click', returnToList); root.querySelector('[data-banner-cancel]')?.addEventListener('click', returnToList); root.querySelector('[data-banner-route-type]')?.addEventListener('change', (event) => root.querySelector('[data-banner-route]')?.toggleAttribute('hidden', !event.target.value)); root.querySelector('[data-banner-test="enabled"]')?.addEventListener('change', (event) => { root.querySelector('[data-banner-test-status]').textContent = event.target.checked ? '生效' : '未启用'; });
    const upload = (key, file) => { if (!file) return; const reader = new FileReader(); reader.addEventListener('load', () => { if (key === 'outboundLogo') record.route = { ...(record.route || {}), outboundLogo: reader.result }; else record[key] = reader.result; this.renderEditor(record, { copy }); this.bind({ mode, recordId, copy }); }); reader.readAsDataURL(file); };
    root.addEventListener('change', (event) => { if (event.target.matches('[data-banner-upload]')) upload(event.target.dataset.bannerUpload, event.target.files?.[0]); }); root.addEventListener('click', (event) => { const button = event.target.closest('[data-banner-delete]'); if (!button) return; const key = button.dataset.bannerDelete; if (key === 'outboundLogo') record.route = { ...(record.route || {}), outboundLogo: '' }; else record[key] = ''; this.renderEditor(record, { copy }); this.bind({ mode, recordId, copy }); });
    root.querySelector('[data-banner-form]').addEventListener('submit', (event) => { event.preventDefault(); const data = new FormData(event.currentTarget); const requiredKeys = ['name', 'sortValue', 'image']; const missing = requiredKeys.find((key) => !String(data.get(key) || '').trim()) || (![...root.querySelectorAll('[data-banner-scene]:checked')].length ? 'scene' : ''); if (missing) return window.BackofficeLayout.showRequiredFieldToast({ name: '记录名称', sortValue: '排序', image: '横幅素材', scene: '横幅场景' }[missing]);
      const testPlan = { uids: String(root.querySelector('[data-banner-test="uids"]')?.value || '').trim(), start: root.querySelector('[data-banner-test="start"]')?.value || '', end: root.querySelector('[data-banner-test="end"]')?.value || '', enabled: Boolean(root.querySelector('[data-banner-test="enabled"]')?.checked) }; const testError = window.ConfigurationSections.validateTestPlan(testPlan); if (testError) return window.BackofficeLayout.showToast('测试计划校验失败', testError);
      const targeting = { ...this.emptyRecord().targeting, ...record.targeting, platformVersions: { ...this.emptyRecord().targeting.platformVersions, ...(record.targeting?.platformVersions || {}) }, identities: [...root.querySelectorAll('[data-banner-identity]:checked')].map((item) => item.value), audiences: [...root.querySelectorAll('[data-banner-audience]:checked')].map((item) => item.value), audienceInversion: root.querySelector('[name="banner-audience-inversion"]:checked')?.value || '否' }; root.querySelectorAll('[data-banner-targeting-field]').forEach((input) => { targeting[input.dataset.bannerTargetingField] = input.value.trim(); }); targeting.status = root.querySelector('[name="banner-status"]:checked')?.value || '待上线'; if (!targeting.onlineStart || !targeting.onlineEnd) return window.BackofficeLayout.showRequiredFieldToast('上下线时间'); if (new Date(targeting.onlineStart).getTime() > new Date(targeting.onlineEnd).getTime()) return window.BackofficeLayout.showToast('上线时间有误', '上下线结束时间不能早于开始时间'); root.querySelectorAll('[data-banner-platform]').forEach((input) => { targeting.platformVersions[input.dataset.bannerPlatform].enabled = input.checked; }); root.querySelectorAll('[data-banner-version]').forEach((input) => { const [platform, boundary] = input.dataset.bannerVersion.split(':'); targeting.platformVersions[platform][boundary] = input.value.trim(); });
      const route = { type: data.get('routeType'), protocol: String(data.get('routeProtocol') || '').trim(), pid: String(data.get('routePid') || '').trim(), selectedPid: data.get('routeSelectedPid'), skipType: String(data.get('routeSkipType') || '').trim(), mallId: String(data.get('routeMallId') || '').trim(), outboundLogo: data.get('outboundLogo'), outboundCopy: String(data.get('routeOutboundCopy') || '').trim() }; const now = new Date(); const next = { ...record, id: record.id || String(Math.max(0, ...rows.map((item) => Number(item.id) || 0)) + 1), name: data.get('name').trim(), sortValue: Number(data.get('sortValue')), scenes: [...root.querySelectorAll('[data-banner-scene]:checked')].map((item) => item.value), image: data.get('image'), darkImage: data.get('darkImage'), route, targeting, onlineAt: targeting.onlineStart, offlineAt: targeting.onlineEnd, status: targeting.status, testPlan, editor: '管理员', updatedAt: now.toISOString().slice(0, 19).replace('T', ' ') }; const index = rows.findIndex((item) => item.id === next.id); if (index >= 0) rows.splice(index, 1, next); else rows.unshift(next); this.write(rows); window.BackofficeLayout.showToast('保存成功', '横幅配置已更新'); returnToList(); });
    window.BackofficeLayout.bindGlobalTooltips();
  }
};
